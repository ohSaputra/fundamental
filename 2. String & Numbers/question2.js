/**
 * Buat input data untuk melakukan kuadrat suatu nilai
 */

const X = prompt('Silahkan masukkan angka berapapun: ')

// tampilkan hasil kuadrat melalui alert

alert('Kuadrat dari ' + X + ' adalah ' + Math.pow(X, 2))